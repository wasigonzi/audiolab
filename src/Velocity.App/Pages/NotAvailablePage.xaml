<Page
    x:Class="Velocity.App.Pages.NotAvailablePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Padding="24">
        <Border Style="{StaticResource GlassCardStyle}" VerticalAlignment="Top">
            <StackPanel Spacing="8">
                <TextBlock Style="{StaticResource SectionTitleStyle}" Text="Not in this build yet" />
                <TextBlock
                    Style="{StaticResource BodyStyle}"
                    Text="This section is part of a later phase. The engine behind it exists and is tested; the page is not written yet." />
            </StackPanel>
        </Border>
    </Grid>
</Page>
