{
  "name": "audiolab",
  "version": "0.1.0",
  "private": true,
  "description": "Servidor de audiolab: API para subir, listar y servir archivos de audio.",
  "type": "module",
  "main": "src/server.js",
  "engines": {
    "node": ">=20.6.0"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^5.2.1",
    "multer": "^2.3.0"
  }
}
